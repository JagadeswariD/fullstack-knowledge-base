## Type Alias:

You can define a base type in which complex type definition is stored and then use type alias instead of repeating the type definition

type Person = {
name:string;
age : number;
}
let person:Person;
Helps in concise and maintaining the code.
